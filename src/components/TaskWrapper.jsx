var React = require('react')

class TaskWrapper extends React.Component {
  constructor (props) {
    super(props)
  }

  render () {
    return (
      <div className='tasks-wrapper u-t-center'>
      Task Wrapper
      </div>
    )
  }
}

module.exports = TaskWrapper
